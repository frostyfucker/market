The application is a full-stack solution. The backend uses Node.js with Express to provide a RESTful API, and it uses a PostgreSQL database for data persistence. User authentication is handled via a custom JWT implementation. The frontend is a single-page application built with React, TypeScript, and styled with TailwindCSS. Images are uploaded to the backend, and all AI interactions are managed through the @google/genai SDK.